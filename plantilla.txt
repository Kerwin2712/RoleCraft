<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RoleCraft - Misión Activa</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <!-- Simulación de HTMX para la plantilla -->
    <script src="https://unpkg.com/htmx.org@1.9.10"></script>
    <style>
        :root {
            --bg-color: #05050f;
            --accent-color: #00f0ff;
            --accent-glow: rgba(0, 240, 255, 0.4);
            --success-color: #00ff88;
            --success-glow: rgba(0, 255, 136, 0.4);
            --text-header: #ffffff;
            --text-muted: #8b949e;
            --glass-bg: rgba(10, 15, 30, 0.6);
            --glass-border: rgba(255, 255, 255, 0.08);
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-header);
            font-family: 'Inter', system-ui, sans-serif;
            background-image: radial-gradient(circle at 50% 0%, rgba(0, 240, 255, 0.05), transparent 50%);
            background-attachment: fixed;
        }

        .glass-panel {
            background-color: var(--glass-bg);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--glass-border);
            border-radius: 1rem;
        }

        .shadow-neon { box-shadow: 0 0 15px var(--accent-glow); }
        .shadow-neon-success { box-shadow: 0 0 15px var(--success-glow); }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: var(--bg-color); }
        ::-webkit-scrollbar-thumb { background: var(--glass-border); border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--accent-color); }

        /* Step Line Connector */
        .step-line::after {
            content: '';
            position: absolute;
            left: 1.25rem; /* center of 2.5rem icon */
            top: 2.5rem;
            bottom: -1rem;
            width: 2px;
            background: var(--glass-border);
            z-index: 0;
        }
        .step-line.active::after {
            background: linear-gradient(to bottom, var(--accent-color), var(--glass-border));
        }
        .step-last::after { display: none; }
    </style>
</head>
<body class="min-h-screen flex flex-col antialiased p-4 md:p-6">

    <!-- Navbar Minimizada -->
    <header class="w-full flex justify-between items-center mb-6 max-w-6xl mx-auto z-10">
        <a href="/dashboard" class="flex items-center gap-2 text-muted hover:text-white transition-colors group">
            <div class="w-8 h-8 rounded-full border border-white/20 flex items-center justify-center group-hover:border-accent group-hover:shadow-neon transition-all">
                <i class="ph ph-caret-left"></i>
            </div>
            <span class="text-sm font-bold tracking-wide uppercase">Volver al Dashboard</span>
        </a>
        <div class="flex items-center gap-4">
            <span class="text-xs text-accent font-mono border border-accent/30 bg-accent/10 px-3 py-1 rounded">
                <i class="ph ph-lightning"></i> 100/100
            </span>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 max-w-6xl mx-auto w-full grid grid-cols-1 lg:grid-cols-3 gap-6 z-10">
        
        <!-- Columna Izquierda: La Ruta de la Misión (2/3) -->
        <section class="lg:col-span-2 flex flex-col gap-6">
            
            <!-- Header de la Misión -->
            <div class="glass-panel p-8 relative overflow-hidden border-l-4 border-l-accent">
                <!-- Background Decoration -->
                <i class="ph ph-terminal-window absolute -right-10 -bottom-10 text-9xl opacity-5 text-accent"></i>
                
                <div class="flex justify-between items-start mb-4 relative z-10">
                    <div>
                        <span class="text-[10px] uppercase font-bold tracking-widest text-accent mb-2 block">Módulo de Configuración</span>
                        <h1 class="text-3xl font-bold mb-2">Lvl 1: Estación de Trabajo</h1>
                        <p class="text-muted text-sm max-w-xl">
                            Un verdadero desarrollador conoce sus herramientas. Tu misión es preparar el entorno local para que podamos comunicarnos. Sigue los protocolos y verifica tu conexión.
                        </p>
                    </div>
                    <div class="flex flex-col items-end gap-2">
                        <span class="text-xs uppercase font-bold tracking-wider bg-green-500/20 text-green-400 px-2 py-1 rounded border border-green-500/20">Fácil</span>
                        <span class="text-sm text-accent font-mono flex items-center gap-1 font-bold"><i class="ph ph-star"></i> +30 XP</span>
                    </div>
                </div>

                <!-- Progress Bar -->
                <div class="mt-6 relative z-10">
                    <div class="flex justify-between text-xs font-bold mb-2">
                        <span class="text-muted uppercase tracking-wider">Progreso de Sincronización</span>
                        <span class="text-accent font-mono" id="progress-text">0 / 3 Pasos</span>
                    </div>
                    <div class="w-full h-1.5 bg-white/10 rounded-full overflow-hidden">
                        <div class="h-full bg-accent w-[10%] shadow-neon transition-all duration-500" id="progress-bar"></div>
                    </div>
                </div>
            </div>

            <!-- Pasos Interactivos (Checklist Gamificado) -->
            <div class="glass-panel p-6">
                <h3 class="text-lg font-bold mb-6 flex items-center gap-2">
                    <i class="ph ph-check-square-offset text-accent"></i> Protocolos Requeridos
                </h3>

                <!-- Paso 1: Activo -->
                <div class="relative step-line active pb-8">
                    <div class="flex gap-4">
                        <div class="w-10 h-10 rounded-full bg-[#05050f] border-2 border-accent flex-shrink-0 flex items-center justify-center shadow-neon relative z-10">
                            <span class="font-bold text-accent text-sm">1</span>
                        </div>
                        <div class="flex-1">
                            <h4 class="font-bold text-white mb-1">Instalar Entorno Virtual</h4>
                            <p class="text-xs text-muted mb-3">
                                Python utiliza entornos virtuales para no contaminar tu sistema. Abre tu terminal en VSCode (Ctrl+Shift+`) y ejecuta el siguiente comando:
                            </p>
                            
                            <!-- Snippet de Código -->
                            <div class="bg-black/60 border border-white/10 rounded p-3 mb-3 flex justify-between items-center group">
                                <code class="font-mono text-xs text-green-400">python -m venv env</code>
                                <button class="text-muted hover:text-white transition-colors" title="Copiar comando">
                                    <i class="ph ph-copy"></i>
                                </button>
                            </div>

                            <!-- Botón de Acción HTMX -->
                            <button 
                                hx-post="/api/verify/step1" 
                                hx-indicator="#terminal-loader"
                                hx-swap="outerHTML"
                                class="bg-accent/10 border border-accent text-accent hover:bg-accent hover:text-black px-4 py-2 rounded text-xs font-bold transition-all flex items-center gap-2">
                                <i class="ph ph-radar"></i> EJECUTAR DIAGNÓSTICO
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Paso 2: Bloqueado -->
                <div class="relative step-line pb-8 opacity-50">
                    <div class="flex gap-4">
                        <div class="w-10 h-10 rounded-full bg-[#05050f] border-2 border-white/20 flex-shrink-0 flex items-center justify-center relative z-10">
                            <i class="ph ph-lock-key text-white/50 text-sm"></i>
                        </div>
                        <div class="flex-1">
                            <h4 class="font-bold text-muted mb-1">Activar el Entorno</h4>
                            <p class="text-xs text-muted mb-3">
                                Completa el paso anterior para desbloquear las instrucciones de activación de Windows/Mac.
                            </p>
                        </div>
                    </div>
                </div>

                <!-- Paso 3: Bloqueado -->
                <div class="relative step-last opacity-50">
                    <div class="flex gap-4">
                        <div class="w-10 h-10 rounded-full bg-[#05050f] border-2 border-white/20 flex-shrink-0 flex items-center justify-center relative z-10">
                            <i class="ph ph-lock-key text-white/50 text-sm"></i>
                        </div>
                        <div class="flex-1">
                            <h4 class="font-bold text-muted mb-1">Instalar Dependencias</h4>
                            <p class="text-xs text-muted mb-3">
                                Descarga los requerimientos del núcleo (requirements.txt).
                            </p>
                        </div>
                    </div>
                </div>

            </div>
        </section>

        <!-- Columna Derecha: Terminal del Sistema (1/3) -->
        <section class="glass-panel flex flex-col h-[500px] lg:h-auto overflow-hidden sticky top-6 border-t-4 border-t-white/10">
            <!-- Terminal Header -->
            <div class="bg-black/40 p-3 border-b border-white/10 flex items-center gap-2">
                <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
                <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
                <span class="text-xs text-muted font-mono ml-2">antigravity_daemon.exe</span>
            </div>
            
            <!-- Terminal Body -->
            <div class="p-4 font-mono text-xs flex-1 overflow-y-auto bg-[#030308] text-muted space-y-2">
                <p>> Inicializando conexión con Agente...</p>
                <p>> Analizando <span class="text-yellow-400">Aprendiz #8492</span>...</p>
                <p class="text-accent">> Estado: Esperando verificación de entorno local.</p>
                <p>> Escuchando peticiones en puerto 8000...</p>
                
                <!-- Loader HTMX target -->
                <div id="terminal-loader" class="htmx-indicator flex items-center gap-2 text-[#00f0ff] mt-4">
                    <i class="ph ph-spinner animate-spin"></i> Analizando telemetría...
                </div>
            </div>

            <!-- Terminal Footer / Final Action -->
            <div class="p-4 bg-black/40 border-t border-white/10">
                <button disabled class="w-full bg-white/5 border border-white/10 text-white/30 py-3 rounded text-sm font-bold uppercase tracking-widest cursor-not-allowed transition-all">
                    Misión Bloqueada
                </button>
            </div>
        </section>

    </main>
</body>
</html>